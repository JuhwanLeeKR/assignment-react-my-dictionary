import React from 'react';
import styled from 'styled-components';

const Header = () => {
  return <Head>My Dictionary</Head>;
};

const Head = styled.header`
  font-size: 40px;
  background: #999;
`;

export default Header;
